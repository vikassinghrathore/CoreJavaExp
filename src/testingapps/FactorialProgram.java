package testingapps;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class FactorialProgram {
    /*static int factorial(int n) {
        if (n == 0) //n=0 or take n==1
            return 1;
            return n * factorial(n - 1);
    }
        public static void main(String[] args) {
        int num=5;
        System.out.println("Factorial of :"+num+"is"+factorial(5));
        System.out.println("Factorial of " + num+ " is " + factorial(5));

        }*/

    public static void main(String[] args) {

        /*List<Integer> number = Arrays.asList(1, 2, 3, 1, 2);
        List<Integer> uniqueval = number.stream().distinct().collect(Collectors.toList());
        System.out.println("Unique value" + uniqueval);
        System.out.println(number);//1, 1, 2, 3, 3, 3, 4, 5, 6, 6, 6, 7, 8
        for (int i = 0; i < uniqueval.size(); ++i) {
            System.out.println(uniqueval.get(i));
    }}}*/

    }}